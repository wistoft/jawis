export {};

const assert = require("assert");

describe("Jest", function () {
  describe("#indexOf()", function () {
    it("igen igen", function () {
      // console.log("in test");
      // assert.strictEqual(1, 2);
      console.log("asdlkfjla");
      // throw new Error("usp");
      // assert.equal(2, 12);

      //goes to failureDetails
      // expect("mikael").toMatchSnapshot();
      // expect("jens").toMatchSnapshot();
    });
  });
});
