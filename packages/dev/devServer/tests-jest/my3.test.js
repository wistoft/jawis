var assert = require("assert");

describe("jest Test3", function () {
  describe("igen", function () {
    it("should something 2", function () {
      // Calling `done()` twice is an error
      // setImmediate(done);
      // setTimeout(() => {
      //   done();
      // }, 500);
      // throw new Error("ups");
    });
  });
});
//
