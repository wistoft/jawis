import { TestProvision } from "^jarun";

// race

export default (prov: TestProvision) => {
  // const waiter = getWaiter(prov, {
  //   onBeforeClearSoftTimeout: () => {},
  // });
  // setTimeout(() => {
  //   waiter.set("done");
  // }, 20);
  // return waiter.await("done", 0, 100);
};
