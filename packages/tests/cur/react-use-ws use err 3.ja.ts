import { TestProvision } from "^jarun";

//sub hook is used twice without unmount in between

//can't implement without a mock.

export default (prov: TestProvision) => {
  // filterReact(prov);
  // const { result } = renderHook(useWebSocketProv, {
  //   URL: "dummy",
  //   reconnect: true,
  // });
  // const { result: result2 } = renderHook(result.useWsEffect, {
  //   onServerMessage: () => {},
  // });
};
